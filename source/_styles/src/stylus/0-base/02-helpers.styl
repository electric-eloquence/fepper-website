/* Clearfix */
.cf
  &::before, &::after
    content: " "
    /* 1 */
    display: table
    /* 2 */
  &::after
    clear: both
@keyframes hiderOut // A helper to the .hider--out transition.
  from
    opacity: 1
  50%
    opacity: 1
  to
    opacity: 0
