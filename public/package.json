{
  "description": "Fepper public directory",
  "scripts": {
    "public:test": "BABEL_DISABLE_CACHE=1 mocha --require @babel/register test/tests",
    "test": "npm run public:test"
  },
  "devDependencies": {
    "fepper-ui": "^0.8.0"
  }
}
