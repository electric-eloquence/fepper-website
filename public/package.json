{
  "description": "Fepper public directory",
  "scripts": {
    "public:test": "mocha --require @babel/register test/tests",
    "test": "npm run public:test"
  },
  "devDependencies": {
    "fepper-ui": "^0.6.1"
  }
}
